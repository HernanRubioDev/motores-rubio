const useApi = ()=>{

  const registerUser = (form)=>{}

  const loginUser = (form)=>{}

  const checkUserName = (username)=>{}

  const checkUserEmail = (username)=>{}

  const checkUserAuth = (username)=>{}

  const addNewMotor = (form)=>{}

  return {registerUser, loginUser, checkUserName, checkUserAuth, addNewMotor}
}

export default useApi;